# Соглашения в коде

## Имена

| Элемент | Правило | Пример | Основание |
|---|---|---|---|
| Модули и функции | `snake_case` | `find_control_topic` | `control.py` |
| Классы | `PascalCase` | `ControlTopic`, `ReminderSpec` | `control.py`, `commands.py` |
| Константы | `UPPER_SNAKE_CASE` | `MISSED_AFTER`, `CONTROL_GROUP` | `scheduler.py`, `config.py` |
| Внутренние функции | начальное `_` | `_process_due`, `_duration_prefix` | `scheduler.py`, `commands.py` |

## Форматирование и проверки

- Линтер и форматтер не настроены `[TODO]`.
- Проверки запускаются стандартным `unittest`; конфигурации покрытия нет `[TODO]`.
- Исходники частично используют аннотации типов; обязательной политики типизации нет `[TODO]`.

## Импорты и модули

- Стандартная библиотека, затем Telethon, затем локальные модули (`main.py`, `scheduler.py`).
- Локальные модули импортируются по имени без алиасов и без `__init__.py`.

## Ошибки и журнал

- Ошибки формата команды возвращаются понятным текстом; ошибки Telegram и планировщика журналируются.
- Ошибки отправки приводят к повтору через 5 минут и отчёту в тему; проблемы сети отмечаются после восстановления.
- Строгих правил маскирования журналов нет `[TODO]`; сообщения об ошибке пользователю включают тип исключения, не его полный текст.

## Тесты

- Файлы названы `tests/test_*.py`; тесты парсера, темы и планировщика используют `unittest` и имитации Telegram-клиента.
- Живые интеграционные тесты с Telegram отсутствуют `[TODO]`.

## Доказательства

- `main.py`
- `commands.py`
- `scheduler.py`
- `control.py`
- `tests/test_commands.py`
- `tests/test_scheduler.py`
